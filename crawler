#!/usr/bin/env python3

import argparse
from html.parser import HTMLParser
from urllib.parse import urljoin, urlsplit, urlunsplit

from http_client import HTTPClient

DEFAULT_SERVER = "fakebook.khoury.northeastern.edu"
DEFAULT_PORT = 443

LOGIN_PATH = "/accounts/login/?next=/fakebook/"
ROOT_PATH = "/fakebook/"
NUM_FLAGS = 5


# grab the href from every <a> tag
class LinkParser(HTMLParser):
    def __init__(self):
        super().__init__()
        self.hrefs = []

    def handle_starttag(self, tag, attrs):
        if tag != "a":
            return
        for name, value in attrs:
            if name == "href" and value:
                self.hrefs.append(value)
                return


# collect text inside <h3 class='secret_flag'> tags
class FlagParser(HTMLParser):
    PREFIX = "FLAG: "

    def __init__(self):
        super().__init__()
        self.flags = []
        self.in_flag = False
        self.buf = []

    def handle_starttag(self, tag, attrs):
        if tag == "h3":
            classes = (dict(attrs).get("class") or "").split()
            if "secret_flag" in classes:
                self.in_flag = True
                self.buf = []

    def handle_endtag(self, tag):
        if tag == "h3" and self.in_flag:
            self.in_flag = False
            text = "".join(self.buf).strip()
            if text.startswith(self.PREFIX):
                self.flags.append(text[len(self.PREFIX):].strip())

    def handle_data(self, data):
        if self.in_flag:
            self.buf.append(data)


def find_csrf_token(html_text):
    # value of the hidden csrfmiddlewaretoken input, or none
    class CsrfParser(HTMLParser):
        def __init__(self):
            super().__init__()
            self.token = None

        def handle_starttag(self, tag, attrs):
            if tag != "input" or self.token is not None:
                return
            attr_dict = dict(attrs)
            if attr_dict.get("name") == "csrfmiddlewaretoken":
                self.token = attr_dict.get("value")

    parser = CsrfParser()
    parser.feed(html_text)
    return parser.token


def path_from_location(location):
    # reduce a location header to just path + query
    parts = urlsplit(location)
    return urlunsplit(("", "", parts.path or "/", parts.query, ""))


class Crawler:
    def __init__(self, args):
        self.server = args.server
        self.port = args.port
        self.username = args.username
        self.password = args.password
        self.client = HTTPClient(self.server, self.port, use_tls=True)

        self.frontier = []
        self.visited = set()
        self.flags = []

    def login(self):
        # get the login page for the csrf token and cookie
        page = self.client.get(LOGIN_PATH)
        csrf_field = find_csrf_token(page.body)
        csrf_cookie = self.client.get_cookie("csrftoken")
        if not csrf_field or not csrf_cookie:
            raise RuntimeError("could not find CSRF token on the login page")

        # need a referer header or the csrf check fails
        referer = "https://%s%s" % (self.server, LOGIN_PATH)
        form = {
            "username": self.username,
            "password": self.password,
            "csrfmiddlewaretoken": csrf_field,
            "next": "/fakebook/",
        }
        response = self.client.post(LOGIN_PATH, form, extra_headers={"Referer": referer})

        # follow the redirect after a good login
        hops = 0
        while response.status in (301, 302) and hops < 10:
            location = response.headers.get("location")
            if not location:
                break
            response = self.client.get(path_from_location(location))
            hops += 1

        if response.status != 200 or self.client.get_cookie("sessionid") is None:
            raise RuntimeError(
                "login failed: status=%d, sessionid=%r"
                % (response.status, self.client.get_cookie("sessionid"))
            )

    def extract_links(self, html_text, base_path):
        parser = LinkParser()
        parser.feed(html_text)
        paths = []
        for href in parser.hrefs:
            path = self._normalize_link(href, base_path)
            if path is not None:
                paths.append(path)
        return paths

    def extract_flags(self, html_text):
        parser = FlagParser()
        parser.feed(html_text)
        return parser.flags

    def _normalize_link(self, href, base_path):
        # resolve href against the current page, keep only same-server /fakebook/ paths
        if not href:
            return None
        base = "https://%s%s" % (self.server, base_path)
        parts = urlsplit(urljoin(base, href))
        if parts.hostname and parts.hostname != self.server:
            return None
        if not parts.path.startswith("/fakebook/"):
            return None
        return urlunsplit(("", "", parts.path, parts.query, ""))

    def crawl(self):
        while self.frontier and len(self.flags) < NUM_FLAGS:
            path = self.frontier.pop()
            if path in self.visited:
                continue
            self.visited.add(path)

            try:
                response = self.client.get(path)
            except (ConnectionError, OSError):
                self.visited.discard(path)
                self.frontier.append(path)
                continue

            status = response.status
            if status == 200:
                for flag in self.extract_flags(response.body):
                    if flag not in self.flags:
                        self.flags.append(flag)
                        if len(self.flags) >= NUM_FLAGS:
                            return
                for link in self.extract_links(response.body, path):
                    if link not in self.visited:
                        self.frontier.append(link)
            elif status in (301, 302):
                location = response.headers.get("location")
                if not location:
                    continue
                target = self._normalize_link(location, path)
                if target and target not in self.visited:
                    self.frontier.append(target)
            elif status in (403, 404):
                continue
            else:
                continue

    def run(self):
        self.login()
        self.frontier.append(ROOT_PATH)
        self.crawl()
        for flag in self.flags[:NUM_FLAGS]:
            print(flag)


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="crawl Fakebook")
    parser.add_argument("-s", dest="server", type=str, default=DEFAULT_SERVER,
                        help="The server to crawl")
    parser.add_argument("-p", dest="port", type=int, default=DEFAULT_PORT,
                        help="The port to use")
    parser.add_argument("username", type=str, help="The username to use")
    parser.add_argument("password", type=str, help="The password to use")
    args = parser.parse_args()
    Crawler(args).run()